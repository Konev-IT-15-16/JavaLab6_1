# Лабораторная работа 6
## Конев Михаил ПР-7
## Вариант 2.
# Задание 1
Аннотации
## Задача 1
## Текст задачи
@Invoke.
Разработайте аннотацию @Invoke, со следующими характеристиками:
* Целью может быть только МЕТОД
* Доступна во время исполнения программы
* Не имеет свойств

Создайте класс, содержащий несколько методов, и проаннотируйте хотя бы один из них
аннотацией @Invoke.

Реализуйте обработчик (через Reflection API), который находит методы, отмеченные
аннотацией @Invoke, и вызывает их автоматически.
## Алгоритм решения
* Создана аннотация @Invoke с:
  * @Target(ElementType.METHOD)
  * @Retention(RetentionPolicy.RUNTIME)
* Разработан класс InvokeReflection:
  * Получает объект
  * Сканирует методы через getDeclaredMethods()
  * Проверяет наличие @Invoke
  * Делает метод доступным через setAccessible(true)
  * Вызывает его через Method.invoke()

* Обработаны возможные исключения

* Рефлексия — это механизм в Java, который позволяет программе анализировать и изменять свое поведение во время выполнения.
С помощью рефлексии можно получать информацию о классах, интерфейсах, полях и методах, а также создавать объекты, вызывать методы и изменять значения полей, даже не зная их имен на этапе компиляции

## Тестирование
* Задача 1.1: Аннотация @Invoke
* В данном задании вам необходимо создать своего кота - ввести имя коту. А мы применим аннотацию @Invoke.
* Введите имя кота: Тишка
* Тишка: мяу!

## Задача 2
## Текст задачи
@Default.
Разработайте аннотацию @Default, со следующими характеристиками:
* Целью может быть ТИП или ПОЛЕ
* Доступна во время исполнения программы
* Имеет обязательное свойство value типа Class
Проаннотируйте какой-либо класс данной аннотацией, указав тип по умолчанию.
Напишите обработчик, который выводит имя указанного класса по умолчанию.
## Алгоритм решения
Реализована обработка аннотации @Default, определяющей класс по умолчанию. 
Класс DefaultReflection с методом showDefault() использует рефлексию: анализирует переданный объект, 
проверяет наличие аннотации @Default и выводит имя класса из её свойства value. 
При отсутствии аннотации выбрасывается IllegalArgumentException, обеспечивая контроль корректности 
использования.

## Тестирование
* Задача 1.2: Аннотация @Default
* В данном задании вам необходимо создать объект Person. 
Мы покажем класс по умолчанию из аннотации @Default.
* Введите имя: Анастасия
* Введите возраст (число): 19

* Введите пароль (только буквы и пробелы): qwerty
* Введите user name (только буквы и пробелы): nastya
* Класс по умолчанию: Person

## Задача 3
## Текст задачи
@ToString.
Разработайте аннотацию @ToString, со следующими характеристиками:
* Целью может быть ТИП или ПОЛЕ
* Доступна во время исполнения программы
* Имеет необязательное свойство valuec двумя вариантами значений: YES или NO
* Значение свойства по умолчанию: YES
Проаннотируйте класс аннотацией @ToString, а одно из полей – с @ToString(Mode.NO).
Создайте метод, который формирует строковое представление объекта, учитывая только те поля,
где @ToString имеет значение YES.
## Алгоритм решения
Реализована аннотация @ToString для управления формированием строкового представления объектов. 
Аннотация применяется к классам и полям, использует Mode.YES (включить) и Mode.NO (исключить). 
Метод showToString() в классе TostringReflection с помощью рефлексии анализирует объект: получает 
его класс и поля, проверяет аннотации, формирует строку в формате имя_поля = значение. 
Приватные поля становятся доступными через setAccessible(true). Если класс не аннотирован или имеет 
Mode.YES, включаются все поля, кроме помеченных Mode.NO.

## Тестирование
* Задача 1.3: Аннотация @ToString
* В данном задании вам необходимо создать объект Person. Мы покажем класс по умолчанию из аннотации @ToString.
* Введите имя: Анастасия
* Введите возраст (число): 19

* Введите пароль (только буквы и пробелы): qwerty
* Введите user name (только буквы и пробелы): nastya resh
* Person [ name = Анастасия age = 19 userName = nastya resh ]
## Задача 4
## Текст задачи
@Validate.
Разработайте аннотацию @Validate, со следующими характеристиками:
* Целью может быть ТИП или АННОТАЦИЯ
* Доступна во время исполнения программы
* Имеет обязательное свойство value, типа Class[]
Проаннотируйте класс аннотацией @Validate, передав список типов для проверки.
Реализуйте обработчик, который выводит, какие классы указаны в аннотации.
## Алгоритм решения
Создана аннотация @Validate для указания связанных классов проверки. 
Класс ValidateReflection с методом showValidation() использует рефлексию: 
анализирует объект, проверяет наличие аннотации @Validate. 
При её отсутствии или пустом массиве классов выбрасывается IllegalArgumentException. 
Выводит названия всех указанных в аннотации классов.

## Тестирование
* Задача 1.4: Аннотация @Validate
* В данном задании вам необходимо создать товар (Product). Мы покажем классы, указанные в аннотации @Validate.
* Введите название товара: Сыр
* Введите цену товара (может быть дробным числом): 200

* Введите количество товара (целое число): 2

* String
* Integer
* Double
 
## Задача 5
## Текст задачи
@Two.
Разработайте аннотацию @Two, со следующими характеристиками:
* Целью может быть ТИП
* Доступна во время исполнения программы
* Имеет два обязательных свойства: first типа String и second типа int
Проаннотируйте какой-либо класс аннотацией @Two, передав строковое и числовое значения.
Реализуйте обработчик, который считывает и выводит значения этих свойств.
## Алгоритм решения
Разработана аннотация @Two для хранения двух значений (строки и числа) над классом. 
Класс TwoReflection с методом ShowTwo() использует рефлексию: получает аннотацию @Two 
из класса объекта и выводит значения first и second в консоль.

## Тестирование
* Задача 1.5: Аннотация @Two
* В данном задании вам необходимо создать своего кота - ввести имя коту. А мы применим аннотацию @Two.
* Введите имя кота: Тишка
* Создан объект: Кот: Тишка
* Извлекаем значения из аннотации @Two:
* Тишка, 7 лет

## Задача 6
## Текст задачи
@Cache.
Разработайте аннотацию @Cache, со следующими характеристиками:
* Целью может быть ТИП
* Доступна во время исполнения программы
* Имеет необязательное свойство value, типа String[]
* Значение свойства по умолчанию: пустой массив
Проаннотируйте класс аннотацией @Cache, указав несколько кешируемых областей.
Создайте обработчик, который выводит список всех кешируемых областей или сообщение, что
список пуст
## Алгоритм решения
Создана аннотация @Cache для указания кэшируемых областей класса. 
Класс CacheReflection с методом showCache() использует рефлексию: проверяет наличие 
аннотации @Cache, при отсутствии или пустом списке областей выбрасывается IllegalArgumentException.
Выводит названия всех указанных кэшируемых областей.

## Тестирование
* Задача 1.6: Аннотация @Cache
* Введите название товара: Помидор
* Введите цену товара (может быть дробным числом): 100.59

* Введите количество товара (целое число): 5

* Создан товар: Помидор, цена: 100.59, количество: 5
* Кэшируемые области: [Товар, Цены, Количество]


# Задание 2
Тестирование
## Задача 2
## Текст задачи
Создайте тест, используя фреймворк JUnit, который проверяет корректность вызова методов,
отмеченных аннотацией @Invoke.
* Использовать Reflection API для поиска методов с аннотацией.
* Убедиться, что метод действительно выполняется без исключений.
* Проверить, что возвращаемое значение или побочный эффект соответствует ожиданиям
(например, устанавливает флаг или изменяет состояние объекта).
* Тест должен использовать аннотацию @BeforeEach для подготовки тестируемого
экземпляра класса.
## Алгоритм решения
Проверена работа метода showInvoke, который автоматически вызывает методы с аннотацией @Invoke.
Первый тест создаёт объект Cat и проверяет, что помеченный метод meow() корректно вызывается 
и выводит ожидаемый текст. Второй тест проверяет обработку объекта без аннотированных методов - 
убеждается, что исключений нет и вывод в консоль отсутствует.

## Тестирование
* Задача 2.2: Тест  @Invoke
* Работу тестирования можно посмотреть в пакете tests/TestInvoke
![testInvoke.png](testInvoke.png)
## Задача 4
## Текст задачи
Написать тест, используя фреймворк JUnit, который проверяет корректность работы механизма
валидации классов, отмеченных аннотацией @Validate.
* Создать класс с аннотацией @Validate, указывающей массив типов для проверки.
* Использовать тест, который вызывает обработчик и проверяет, что список классов,
переданный в аннотации, корректно извлекается и при передаче пустого массива
выбрасывается исключение IllegalArgumentException.
* В тесте использовать аннотацию @Test с параметром expected (или assertThrows в JUnit 5).
## Алгоритм решения
Проверена работа метода showValidation, который выводит имена классов из аннотации @Validate.
Первый тест создаёт объект с аннотацией и проверяет корректность вывода классов. 
Второй тест проверяет обработку ошибки при пустом массиве классов - ожидается исключение
IllegalArgumentException.

## Тестирование 
* Задача 2.4: Тест @Validate
* Работу тестирования можно посмотреть в пакете tests/TestValidate
![testValidate.png](testValidate.png)
